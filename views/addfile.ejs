<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
    <title>Create New File</title>
    <style>
      body {
        font-family: "Inter", sans-serif;
      }
    </style>
  </head>
  <body class="flex items-center justify-center min-h-screen bg-slate-100 p-5">
    <div
      class="bg-slate-200 p-8 rounded-lg shadow-lg w-full md:max-w-3xl sm:max-w-md text-gray-700"
    >
      <form
        action="/file/new/<%= folderId %>"
        method="post"
        enctype="multipart/form-data"
      >
        <h3 class="text-center text-3xl font-bold mb-8">Upload File</h3>
        <div class="mb-3">
          <label for="FolderName" class="block text-sm font-semibold mb-2"
            >File</label
          >
          <input
            type="file"
            class="rounded-md px-4 py-3 w-full border text-sm cursor-pointer"
            name="myFile"
            required
          />
        </div>
        <button
          class="bg-gray-700 text-white py-3 w-full rounded-md hover:bg-slate-950"
        >
          Upload
        </button>
      </form>
    </div>
  </body>
</html>
